# Supabase migrations

These SQL files re-establish version control for schema/RLS/trigger/storage
changes (the historical migrations were reset in the 2026-07 cleanup).

## Baseline snapshot

`00000000000000_baseline.sql` is a machine-generated `pg_dump --schema-only`
snapshot of the live `public` schema, regenerated 2026-09-24 (it had stood at
its original 2026-07-10 capture until then, stale by every migration from 09 to
21), captured **after** the dated change-migrations below were applied. It is the version-controlled source of
truth for the current DB structure (tables, RLS, triggers, functions, grants) —
for drift detection, schema review, and disaster recovery. It intentionally
overlaps with the dated migrations, which remain the reviewable changelog of how
the security posture got here. Regenerate it after future schema changes with **`npm run db:schema`**
(`scripts/backup/regenerate_baseline.mjs`, which runs that `pg_dump` and
normalises the two things that change on every run — the version header and
pg_dump 17's random `estrict` token — so the diff shows only real drift).
`npm run db:schema:check` is the same thing as a drift detector: non-zero exit
means the live schema is not what these files say it should be.

**The dump deliberately includes ownership and privileges.** The script passed
`--no-owner --no-privileges` until 2026-09-24, which meant the first person to
regenerate would have silently dropped 123 `GRANT`/`REVOKE` statements from the
file this README calls the source of truth for grants. Review a regenerated
baseline for *removals*, not just additions: on this project a disappearing
grant is a security change, and the only ones that should ever vanish are the
ones a migration deliberately revoked (migration 12 removed twelve).

Note the dump is **schema only**. It is not a backup of the data, and the
project is on the Supabase free tier, which has none of its own. Data recovery
comes from the nightly snapshots described in
[`docs/DISASTER-RECOVERY.md`](../../docs/DISASTER-RECOVERY.md).

## Migration execution policy

As authorized by the project owner on 2026-09-19, **agents may review, execute,
and verify migrations themselves**, including on the shared production database,
as part of assigned work. A separate owner application step or repeat permission
request is not required. This supersedes the owner-only execution instructions
in historical plans and migration comments.

The Supabase project is a **single shared instance**: running SQL here affects
**production immediately**. Migrations are deliberately executed, not blindly
auto-applied in filename order. The executing agent must:

1. Inspect live schema state and dependencies; preserve other agents' work.
2. Review the SQL and validate affected permissions/behavior in isolation.
3. Deploy and verify prerequisite application changes before incompatible SQL.
4. Capture the affected schema/configuration for recovery by taking a tagged
   snapshot first — `npm run db:snapshot -- --tag pre-migration-NN` — rather
   than hand-written notes; use transactions and lock/statement timeouts where
   supported. Avoid customer-data mutations unless needed for the assigned
   migration. See [`docs/DISASTER-RECOVERY.md`](../../docs/DISASTER-RECOVERY.md)
   for what to do when one of these goes wrong.
5. Apply only the pending migration, then verify the live result and affected
   workflows. Record the file checksum, application time, deployment revision,
   verification results, and any remaining limitations in the release record
   and roadmap. Do not mark unperformed checks complete.

This authority does not authorize unrelated destructive data operations. If a
required deployment or connection is unavailable, resolve that dependency or
report the actual blocker rather than bypassing the release sequence.

## P0 security migrations (2026-07-10)

Apply in order. Read the header comment in each file first.

| File | Fixes | Notes |
|---|---|---|
| `20260710_01_harden_handle_new_user.sql` | #3 admin self-escalation | Safe to apply as-is. After applying, verify a signup with `data:{role:'admin'}` yields `role='user'`. Includes an optional demote-existing-admins block — review before running. |
| `20260710_02_restrict_partner_brands_read.sql` | #6 partner_brands PII leak | Non-destructive column-level `REVOKE`/`GRANT` (no data moved). Ship the coordinated code first: `getActiveBrands()` selects display columns only, admin editor uses `getAdminBrands()` (service role). On the shared DB, deploy that code to **production** before applying, or prod's old `select('*')` on `partner_brands` breaks. Reversible via the rollback line in the file. |
| `20260710_03_lock_studio_wardrobe_bucket.sql` | #7 world-readable wardrobe photos | **Coordinate with code.** Makes the bucket private + authenticated-only (no anon read/write) → `getPublicUrl` links break, so the app now renders signed URLs (`lib/wardrobe-images.ts`, wired into the wardrobe/lookbook components on this branch). Deploy that code to production and verify wardrobe images load, THEN apply. Uses authenticated-only (not strict per-owner) because the upload folder convention is inconsistent — see the note in the file and the AUDIT P2 item. |
| `20260710_04_email_rate_limits.sql` | P1 email rate limiting | Additive (new `rate_limits` table + `check_rate_limit` function). Safe to apply anytime; the app fails open if it's absent, so code can ship first. Reversible: `DROP FUNCTION public.check_rate_limit; DROP TABLE public.rate_limits;` |
| `20260710_05_stripe_processed_events.sql` | P1 webhook idempotency | Additive (new `stripe_processed_events` table). Safe to apply anytime; the webhook fails open if it's absent, so code can ship first. Reversible: `DROP TABLE public.stripe_processed_events;` |

## September authorization release

The September assessment found additional authorization gaps after the July
fixes. See [release dependencies and verification](../../docs/RELEASE-2026-09-19-AUTHORIZATION.md).

| File | Status / dependencies |
|---|---|
| `20260711_06_lookbooks_canvas_columns.sql` | Recorded applied in roadmap; baseline overlaps these historical changes. |
| `20260711_07_owner_scope_studio_wardrobe.sql` | Owner/admin policy present in live September inspection. |
| `20260715_08_private_internal_note.sql` | Private-column restriction present in live September inspection. |
| `20260911_10_catalog_publication.sql` | Publication columns present live. **Dependency of 09**, despite filename order. |
| `20260911_09_chapter_video_entitlement.sql` | Video-column restriction and entitlement RPC present live; requires 10. |
| `20260911_11_founding_cohort_grants.sql` | Cohort schema present live; this does not establish content/payment launch readiness. |
| `20260919_12_authorization_boundaries.sql` | **Applied and verified 2026-09-20 03:53 UTC (September 19 EDT)** by the agent, after production release `87dea9e`. All 13 structural and 33 live smoke checks pass; temporary fixtures removed. See the release verification record. |
| `20260920_13_purchase_claims.sql` | **Applied and verified 2026-09-20 17:0x UTC** by the agent. Fixes F06. Purely additive (new `purchase_claims` table), so it was safe to apply ahead of the code that uses it; the code fails closed if the table is absent. sha256 `5ea5ae2ff3d04bbd8b058e1e0b3a1b66398009f8d67ade34ef9b7f6b2cab1cda`. Dry-run first in a rolled-back transaction, then committed. Verified live: table present, RLS enabled **and** forced, 0 policies, 0 grants to `anon`/`authenticated`/`PUBLIC`, 4 indexes, and the single-use property (first consume returns 1 row, a second returns 0) — probe row removed in both runs. No backfill: 0 accounts carried `pending_password` and 0 were in the exploitable "has a password but still flagged claimable" state. Reversible: `DROP TABLE public.purchase_claims;` |
| `20260920_14_fulfillments.sql` | **Applied and verified 2026-09-20** by the agent. Completes F05. Additive: new `fulfillments` table (one row per Stripe line item, unique on `stripe_line_item_id`, status `processing`/`completed`/`failed`/`unfulfillable`), plus a nullable `stripe_line_item_id` column and a **partial** unique index on `purchases` — partial so the 5 pre-existing rows (all null) are unaffected. Dry-run first in a rolled-back transaction, then committed. Verified live: RLS enabled **and** forced, 0 policies, 0 grants to `anon`/`authenticated`/`PUBLIC`, and five behaviour probes — a duplicate line item is rejected, a completed row cannot be re-claimed, an invalid status is refused by the check constraint, a duplicate purchase for one line item is rejected, and legacy null-line-item rows still coexist. All probe rows removed in both runs. Reversible: `DROP TABLE public.fulfillments; DROP INDEX IF EXISTS public.purchases_line_item_unique; ALTER TABLE public.purchases DROP COLUMN IF EXISTS stripe_line_item_id;` |
| `20260920_15_account_deletion.sql` | **Applied and verified 2026-09-20** by the agent. Fixes F11 **and a gap the finding missed**: `public.profiles` had no FK to `auth.users` at all, so a "successful" account deletion left the profile and everything cascading from it. Proven on the live DB first: deleting a user with course progress failed on `user_progress_user_id_fkey`. Sets `user_progress.user_id` to CASCADE, `tailor_cards.last_updated_by` to SET NULL (audit attribution — cascading would delete a *client's* card because a *stylist* left), removes 10 orphaned `@example.invalid` fixture profiles left behind by migration 12's own cleanup, and adds `profiles.id -> auth.users(id) ON DELETE CASCADE`. Dry-run first; the probe deleted a user with 9 progress rows and 10 essence responses and confirmed profile, progress and essence all went with them, then rolled back. After apply: 16 profiles, **0 orphaned, 0 blocking FKs to `auth.users`**. **Caveat recorded in the file:** the applied DELETE guard enumerated seven child tables and missed `tailor_cards`, so two fixture-owned tailor cards cascaded; checked afterwards and both surviving cards belong to real accounts, so nothing of value was lost. The committed SQL now includes that guard. |
| `20260920_16_intake_token_expiry.sql` | **Applied and verified 2026-09-20** by the agent. Completes F10. Adds `wardrobes.upload_token_expires_at`; intake links now last **7 days** (owner's decision). Additive, one nullable column. Both live tokens dated from January/February, so dating expiry from `created_at` would have expired every existing link on apply — they were given a fresh week instead, verified by the dry run (`tokens left immediately expired: 0`). NULL means no expiry and is honoured, so a row missing the backfill fails open rather than locking a client out; every code path that issues or rotates a token now sets one. The upload cap (`MAX_ITEMS_PER_WARDROBE`, 500) is deliberately in code, not here, so it can be tuned without a migration once there is usage data. sha256 `75d23b407a22fac92d5230736130efe795594adb7c1794b3ff861f4c57bda457`. Reversible: `ALTER TABLE public.wardrobes DROP COLUMN upload_token_expires_at;` |
| `20260920_17_catalog_data_repair.sql` | **Applied and verified 2026-09-20** by the agent. Phase 3.6 item 1 — the gate on content work. Exporting the live catalogue and feeding it back to `import_catalog.mjs` reported **24 errors**, so the tool the content work depends on rejected the content it had just exported. Repairs two data defects: **8 Essence Lab questions across 7 chapters** carried the legacy `question` key with no `label`, and the Lab renders `label`, so those prompts rendered **blank** (all affected chapters unpublished, so no customer had met one — it fires on publish); and **6 modules** had `masterclass_id` set with `is_standalone = true`, which cannot both hold. Dry-run verified the rewrite preserves element order, element count and prompt text verbatim, changing only the key name. **Deliberately untouched:** `unit-1-2-universal-styles` has both a working `label` and a different `question`, so renaming would overwrite editorial copy with editorial copy — left for a content edit. After apply the round-trip reports **10 errors, all `video_id` placeholders on the 5 published modules — i.e. F09, the content blocker, and nothing else.** No automatic rollback; the pre-state is the `content/catalog/catalog.json` committed in `4b6c2bc`. |
| `20260921_18_rename_style_essence.sql` | **Applied and verified 2026-09-21 19:01 UTC** by the agent. Renames the masterclass "The AC Method" to "Style & Essence" **in place** (same id `b4d0040b-…`). Needed because `import_catalog.mjs` matches masterclasses by title and never deletes: renaming in the file alone would insert a duplicate and leave the old row rendering as an "In production" card. Pre-check: a scan of every text/json column in `public` found "AC Method" only in `masterclasses.title`. The DO block refuses to commit unless exactly one row changed. The new copy, the reorder (Style & Essence 3, Closet Curation 4) and the new module `defining-your-essence` were then written by the importer (0 errors; 0 new masterclass rows), and a fresh export round-trips byte-identical to `content/catalog/catalog.json`. sha256 `ac6cc2c514788902fa9f404c463597927d12496b622b71a3427260b0292975f0`. Reversible: `UPDATE public.masterclasses SET title = 'The AC Method' WHERE title = 'Style & Essence';` |
| `20260921_19_masterclass_pass.sql` | **Applied and verified 2026-09-21 22:12 UTC** by the agent. Adds the Masterclass Pass entitlement: `profiles.has_masterclass_pass` (boolean, not null, default false) and one `check_access()` branch — with the pass, a masterclass or a module belonging to one is accessible; a standalone course is not. Covers masterclasses published after purchase (owner decision), hence a flag rather than per-masterclass grants. The `offers` row is **not** inserted here; it is created in admin, whose generator creates the Stripe product. A guard refuses to replace `check_access()` unless the live body still carries the baseline clauses — it did; the pre-state definition was captured before apply and matched between dry run and apply. Dry-run first in a rolled-back transaction, then committed, then re-verified in a separate transaction: column present; `authenticated`/`anon` hold no INSERT/UPDATE on it; 0 profiles flagged; function ACL unchanged; behaviour probe on a user with no grants — without the pass masterclass/module/standalone = false/false/false, with it (set inside the probe, rolled back) = **true/true/false**; a full-unlock user still opens a standalone course. sha256 `3504200d29eaa288df7a5d061bed5b37ad2bb3f7fb9859b467305600bcd3ef82`. Rollback: re-run the baseline `check_access` definition, then `ALTER TABLE public.profiles DROP COLUMN has_masterclass_pass;` |
| `20260922_20_masterclass_resources.sql` | **Applied and verified 2026-09-23 19:5x UTC** by the agent. Adds `masterclasses.resource_urls` (jsonb, NOT NULL DEFAULT `'[]'`), so a downloadable that belongs to a whole masterclass no longer has to be duplicated onto every module. Purely additive, one column. No grant change was needed and that was checked rather than assumed: `masterclasses` still carries a plain **table-level** SELECT grant (unlike `chapters`, which migration 09 converted to column-level, where a new column would be invisible until named), so all four grantees were identical before and after and an anonymous `select('*')` still succeeds. Dry-run first in a rolled-back transaction, then committed, then re-verified on a fresh connection: column present and NOT NULL with the `'[]'` default, all 4 masterclasses default to `[]`, and a write/read probe round-tripped a `{name, url}` object and was cleaned up (0 rows left non-empty in both runs). sha256 `b7171b3f1c2de669670c5abb89ce0a127c8a55f9436cfc1e37fa4ebf51a42909`. **Limitation carried forward, recorded in the file header:** the column is anonymously readable over PostgREST, exactly like `chapters.resource_urls` (migration 09's own "STILL PUBLIC" note) — the entitlement check on the new panels is UI-level, matching the courses page today. Reversible: `ALTER TABLE public.masterclasses DROP COLUMN resource_urls;` |
| `20260925_22_check_access_boundaries.sql` | **Applied and verified 2026-09-26.** Applied 2026-09-26 00:19 UTC by the agent, after the owner's user wipe (2 admin accounts left), with snapshot `backups/pre-migration-22--2026-09-26T001755Z` (auth=full, sha256 `9084159…642c`). Procedure for all four: dry-run in one rolled-back transaction with live role probes (17/17 pass); the same probes run WITHOUT the migrations failed on 9 of 17, proving they discriminate; then each file committed as written; then re-probed on a fresh connection (17/17) and `verify_authorization.mjs` 0 failures; probe fixtures confirmed gone (0 rows). Proven first in PGlite: `tests/integration/`. `check_access` answers only for the caller (service role exempt), `anon` loses EXECUTE, and the Course Pass branch also requires `masterclass_id IS NULL`, so a module left with the `is_standalone` default is not a course. Live probes: a course-pass member opens a standalone course but not a mis-flagged module; a member asking about another user gets false; anon gets 42501; service role and an admin still answer. sha256 `38a1f13ddb2f9b2483377ff36c32e51a29600a8255e4c5a14fd667acffe1ca13`. Rollback in the file header. |
| `20260925_23_grant_uniqueness_and_studio_unlock.sql` | **Applied and verified 2026-09-26.** Applied 2026-09-26 00:19 UTC by the agent, after the owner's user wipe (2 admin accounts left), with snapshot `backups/pre-migration-22--2026-09-26T001755Z` (auth=full, sha256 `9084159…642c`). Procedure for all four: dry-run in one rolled-back transaction with live role probes (17/17 pass); the same probes run WITHOUT the migrations failed on 9 of 17, proving they discriminate; then each file committed as written; then re-probed on a fresh connection (17/17) and `verify_authorization.mjs` 0 failures; probe fixtures confirmed gone (0 rows). Proven first in PGlite: `tests/integration/`. Unique indexes on `user_access_grants (user_id, masterclass_id)` and `(user_id, chapter_id)` — without them the renewal branch of `grantItemForTerm` never ran; duplicate check found 0 after the wipe. `handle_new_purchase` also matches `services.stripe_product_id`, so a service marked as unlocking the Studio does. Live probes: a second grant for one masterclass is 23505; buying a Studio service by product id sets `active_studio_client`. No backfill (users wiped). sha256 `d1ca1b093d9cb7a037fbe6efcfe8995e03c2be00c4ac96924b0edd5f808b30ac`. |
| `20260925_24_wardrobe_item_insert_scope.sql` | **Applied and verified 2026-09-26.** Applied 2026-09-26 00:19 UTC by the agent, after the owner's user wipe (2 admin accounts left), with snapshot `backups/pre-migration-22--2026-09-26T001755Z` (auth=full, sha256 `9084159…642c`). Procedure for all four: dry-run in one rolled-back transaction with live role probes (17/17 pass); the same probes run WITHOUT the migrations failed on 9 of 17, proving they discriminate; then each file committed as written; then re-probed on a fresh connection (17/17) and `verify_authorization.mjs` 0 failures; probe fixtures confirmed gone (0 rows). Proven first in PGlite: `tests/integration/`. The member item-insert policy also requires `wardrobe_id` to be NULL or a wardrobe the caller owns. Live probes: a member planting an item in another wardrobe gets 42501; adding to her own still works. sha256 `d62e718afeb126a449df1711e12400e51b9008082a50e887e1abf263ed3efb27`. |
| `20260925_25_get_user_role_caller_only.sql` | **Applied and verified 2026-09-26.** Applied 2026-09-26 00:19 UTC by the agent, after the owner's user wipe (2 admin accounts left), with snapshot `backups/pre-migration-22--2026-09-26T001755Z` (auth=full, sha256 `9084159…642c`). Procedure for all four: dry-run in one rolled-back transaction with live role probes (17/17 pass); the same probes run WITHOUT the migrations failed on 9 of 17, proving they discriminate; then each file committed as written; then re-probed on a fresh connection (17/17) and `verify_authorization.mjs` 0 failures; probe fixtures confirmed gone (0 rows). Proven first in PGlite: `tests/integration/`. `get_user_role` returns NULL unless asked about the caller (service role exempt); EXECUTE stays with anon because RLS policies call it. Live probes: anon and a member learn nothing about the admin; a member still sees her own role; service role still answers; admins still see other profiles through the policy; a member still cannot. sha256 `3e9881ec50242a9d9745db44773a57953a34ad1f693328dc4f967188c82df83e`. |
| `20260926_26_trusted_logos_admin_write.sql` | **Applied and verified 2026-09-26 01:25 UTC** by the agent. Fixes SEC-002 from the 2026-09-25 external assessment (`docs/archive/ENG_ASSESSMENT.MD`): the "admin write" policy on `trusted_by_logos` checked only `auth.role() = 'authenticated'`, so any account could add, replace or delete the public homepage logos. Now requires `profiles.role = 'admin'` for `auth.uid()`. Snapshot `backups/pre-migration-26--2026-09-26T012427Z` (auth=full, sha256 `1417…4b40`). Live probes WITHOUT the migration: a random signed-in member could insert, update and delete a logo (the hole is real in production). Dry run in a rolled-back transaction, then committed, then re-probed on a fresh connection: member insert 42501, member update/delete 0 rows, admin insert and update still work, anon insert 42501, anon still reads all 12 logos; 0 probe rows left. `verify_authorization.mjs` now checks this policy too: 0 failures. Baseline regenerated; its diff is exactly this policy. sha256 `d268a3cd97840738afcc20d0a4f36c2f7b99510c1ae078927fbff70fc601032c`. Rollback in the file header. |
| `20260926_27_term_line_item.sql` | **Applied and verified 2026-09-26 02:22 UTC** by the agent. Fixes PAY-001 from the 2026-09-25 external assessment: the grant and "line item completed" were separate writes, so a run that died between them was re-claimed after 15 minutes and granted a second year. Adds nullable `profiles.access_term_line_item` and `user_access_grants.term_line_item`; `app/lib/access-logic.ts` stamps the line item in the same compare-and-set write that extends the term and skips a line item already stamped. Additive; the code that writes the columns must deploy **after** this (it did not exist in production at apply time). Snapshot `backups/pre-migration-27--2026-09-26T022101Z` (auth=full, sha256 `38a0…d854`). Dry run in a rolled-back transaction, then committed, then re-probed on a fresh connection, both times with a throwaway member created inside the rolled-back transaction (0 left afterwards): both columns present, nullable, no default; the member gets 42501 writing her own `access_term_line_item` and changes 0 rows of `user_access_grants`; the service role writes; 0 rows stamped. `verify_authorization.mjs` 0 failures. Baseline diff is exactly the two columns and their comments. sha256 `3698de1f6e7c4212f92565fb86e06cc73cdd6c3ca4d9082b8e386895a522d63a`. Rollback in the file header. |
| `20260926_28_wardrobe_token_expiry_and_assignment.sql` | **Applied and verified 2026-09-26 03:38 UTC** by the agent. Fixes STUDIO-001/002 from the 2026-09-25 external assessment: four wardrobe insert paths left the bearer intake token with a NULL (never-expiring) expiry, and `assignWardrobe` made three unchecked writes. `upload_token_expires_at` gets `DEFAULT now() + 7 days` and NOT NULL (0 NULL rows live, so the backfill was a no-op); `assign_wardrobe(uuid, uuid)` does owner, Studio flag and items in one transaction, raising P0002 for a missing wardrobe or profile, EXECUTE for service_role only. Pre-state: column nullable with no default, function absent. Snapshot `backups/pre-migration-28--2026-09-26T033759Z` (auth=full, sha256 `91c2…ade8`). Dry run in a rolled-back transaction, then committed, then re-probed on a fresh connection, with a throwaway member, intake wardrobe and item created inside the rolled-back transaction (0 left): raw insert gets a 7-day expiry; explicit NULL is 23502; member and anon get 42501 on the function; service role assigns owner, Studio flag and the item; a missing wardrobe is P0002. `verify_authorization.mjs` 0 failures. Baseline diff is exactly the column, the function, its comment and its grants. Deployed code works with it (the default fills the expiry it omits); the new code requires it. sha256 `570be932127e6fdae099d281dd0657fca75ac24d7b9b4b293fd0d9a90cbe16f8`. Rollback in the file header. |

Do not blindly replay the baseline and dated files against a fresh or live DB.
They overlap, and filename order is not a complete dependency plan. Migration 12
was tested against an isolated fixture of the affected live authorization state.
After application, run `node scripts/verify_authorization.mjs` and the
release smoke checks before marking it verified in production.

## Verifying #3 after apply

```sql
-- Confirm the trigger function no longer trusts client role metadata:
SELECT prosrc FROM pg_proc WHERE proname = 'handle_new_user';
-- Confirm no unexpected admins:
SELECT id, email, role FROM public.profiles WHERE role = 'admin';
```
